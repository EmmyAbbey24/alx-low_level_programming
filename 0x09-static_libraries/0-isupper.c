#include "main.h"

/**
 * _isupper - uppercase letters
 * @o: char to check
 *
 * Return: if upcase is 1, else return 0
 */

int _isupper(int o)

{
if (o >= 'A' && o <= 'Z')

return (1);

else

return (0);

}
